const { logger } = require('../util/logger');
const { submitTicket,
    getPendingTickets,
    processTicket,
    viewTicketsAsEmployee } = require('../services/ticketService');

const submitTicket = async (req, res) => {
    try {

    } catch (error) {

    };
};

const getPendingTickets = async (req, res) => {
    try {

    } catch (error) {

    };
};


const processTicket = async (req, res) => {
    try {

    } catch (error) {

    };
};


const viewTicketsAsEmployee = async (req, res) => {
    try {

    } catch (error) {

    };
};

module.export = {
    submitTicket,
    getPendingTickets,
    processTicket,
    viewTicketsAsEmployee
};